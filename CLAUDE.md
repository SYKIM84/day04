# 김상연 포트폴리오

디자인 작업물을 소개하는 개인 포트폴리오 사이트.

## 목적

- 자신의 디자인 작업물을 한곳에 모아 보여주고, 방문자가 작업물을 둘러볼 수 있게 한다.
- 작업물은 사이트 주인이 직접 로그인해서 등록·수정·삭제한다.

## 핵심 기능

| 기능 | 설명 | 접근 |
|------|------|------|
| 자기소개 | 이름, 소개 글, 연락처 등 기본 정보 | 누구나 |
| 작업물 목록 | 등록된 작업물을 목록으로 보여줌 | 누구나 |
| 작업물 상세 | 작업물 하나의 이미지, 설명, 사용 도구 등 | 누구나 |
| 로그인 | 사이트 주인만 로그인 (Supabase Auth 사용 예정) | 주인 |
| 작업물 등록/수정/삭제 | 로그인한 주인만 작업물을 관리 | 주인 |

## 기술 스택

- 프론트엔드: 순수 HTML, CSS, JavaScript. 빌드 도구나 프레임워크 없이 정적 파일로 구성한다.
- 백엔드/데이터: Supabase (PostgREST API, Auth).
  - 프로젝트 URL: `https://rtpjjdraobpkymydffzb.supabase.co`
  - REST 엔드포인트: `https://rtpjjdraobpkymydffzb.supabase.co/rest/v1/`
  - 브라우저에서는 anon(publishable) 키만 사용한다. 이 키는 공개돼도 되는 키이므로 HTML에 직접 넣어도 된다.
  - service_role 같은 secret 키는 절대 HTML이나 저장소에 넣지 않는다.
  - 데이터 보호는 Supabase의 RLS(Row Level Security) 정책으로 한다. 등록/수정/삭제는 로그인한 주인만 가능하도록 정책을 설정해야 한다.
- 배포: GitHub 저장소 `SYKIM84/day04`, `main` 브랜치. GitHub Pages로 정적 배포 예정.
- 글꼴: Google Fonts의 IBM Plex Sans KR.

## 현재 파일 구성

| 파일 | 역할 |
|------|------|
| `index.html` | 첫 화면. 페이지 목록으로 연결한다. |
| `preview.html` | Supabase `test_todos` 표를 불러와 목록으로 보여주는 연습용 페이지. Supabase 연동 방식의 참고용이다. |
| `CLAUDE.md` | 이 문서. |

`preview.html`은 포트폴리오 기능이 아니라 Supabase 연동을 연습한 페이지다. 작업물 목록 페이지를 만들 때 fetch 방식과 상태 처리(로딩, 빈 상태, 오류)를 참고하면 된다.

## 디자인 체계

전체 페이지는 어두운 남색을 기준으로 한다. 새 페이지를 만들 때 아래 토큰과 원칙을 그대로 따른다.

### 색

```css
--navy-900: #0E1729;   /* 바탕 */
--navy-800: #16233D;   /* 살짝 올라온 면 (코드 블록, 입력창 등) */
--navy-700: #1E2F52;   /* 비활성 요소 */
--line: rgba(156, 199, 255, 0.16);         /* 구분선 */
--line-strong: rgba(156, 199, 255, 0.34);  /* 강한 구분선, 테두리 */
--text: #E6ECF7;       /* 본문 */
--muted: #93A4C4;      /* 보조 글자 */
--accent: #9CC7FF;     /* 링크, 버튼 (달빛 파랑) */
--done: #7ED9B0;       /* 완료, 성공 */
--warn: #FFB18A;       /* 주의, 오류 */
```

### 원칙

- 글꼴은 IBM Plex Sans KR 하나만 쓴다. 본문 15px, 큰 제목 34px(모바일 27px), 항목 제목 19px.
- 카드 상자와 그림자를 쓰지 않는다. 항목은 가는 구분선(`--line`)으로 나눈다.
- 큰 제목은 "할 일 3개 중 2개를 마쳤습니다"처럼 내용을 담은 문장으로 쓴다. 숫자만 크게 띄우거나 단어 하나만 다른 색으로 강조하지 않는다.
- 라벨은 대문자나 눈썹 제목(eyebrow)을 쓰지 않는다. 한글 라벨은 자연스러운 단어로 쓴다.
- 한글 제목은 `word-break: keep-all`로 어절 단위 줄바꿈을 한다.
- 움직임은 페이지당 하나만 두고, `prefers-reduced-motion`을 존중한다.
- 키보드 포커스는 `:focus-visible`로 강조색 외곽선을 보여준다.
- 화면 폭 480px 이하에서도 깨지지 않게 만든다. 좌우 여백 16px.
- 빈 상태와 오류 안내는 무엇을 하면 되는지 알려주는 문장으로 쓴다. 사과하거나 모호하게 쓰지 않는다.

## 작업 규칙

- 커밋 메시지는 한글로 쓴다. 첫 줄에 무엇을 했는지 요약하고, 필요하면 빈 줄 뒤에 항목별 설명을 붙인다.
- 커밋과 푸시는 사용자가 요청할 때만 한다.
- 페이지를 수정하면 브라우저에서 실제로 열어 확인한 뒤 완료로 보고한다.
  - 로컬 확인: 프로젝트 폴더에서 `python -m http.server 8765` 실행 후 `http://localhost:8765/` 접속.
- 새 페이지를 만들면 `index.html`의 페이지 목록에 링크를 추가한다.
- 응답과 문서, UI 문구는 모두 한국어로 쓴다.

## 폰트 규칙

- 폰트는 컬러를 green 컬러로 설정한다.

## 앞으로 할 일

- Supabase에 작업물 표(예: `works`) 설계: 제목, 설명, 이미지 URL, 사용 도구, 제작일, 정렬 순서.
- Supabase Storage에 작업물 이미지 업로드.
- Supabase Auth로 로그인 페이지 구현. 주인 계정 하나만 허용.
- RLS 정책: 읽기는 누구나, 쓰기는 로그인한 주인만.
- 자기소개, 작업물 목록, 작업물 상세, 작업물 관리(등록/수정/삭제) 페이지 제작.
- GitHub Pages 배포 설정.
